import { Button } from "@chakra-ui/react";

const navbarDropButton=()=>{
    return(
       <Button
       isLoading
       loadingText="Signing In"
       variant="ghost"
       colorScheme="WhiteAlpha"
       >
       </Button>
    );
}
export default navbarDropButton;